<UserControl x:Class="TileDefEditor.Views.TileDefList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TileDefEditor.Views"
			 xmlns:vm="clr-namespace:TileDefEditor.ViewModels"
			 Width="Auto">
	<DockPanel Background="#222">

		<StackPanel Orientation="Horizontal" Margin="10" DockPanel.Dock="Top">
			<TextBlock Text="Tile Types"
					   Foreground="White"
					   FontWeight="Bold"
					   VerticalAlignment="Center" />
			
			<!-- Add button bound to MainViewModel.AddTileCommand -->
			<Button Content="Add" Margin="10,0,0,0" Width="60" Command="{Binding AddTileCommand}" />
		</StackPanel>

		<ListBox ItemsSource="{Binding TileList.Tiles}"
				 SelectedItem="{Binding TileList.SelectedTile}"
				 DisplayMemberPath="Name"
				 Background="#1E1E1E"
				 Foreground="White" />
	</DockPanel>
</UserControl>
